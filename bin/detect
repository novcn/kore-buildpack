#! /usr/bin/env bash

exit 0

#if [[ -f "$1/cppcms.js" ]] ; then
#  SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#  echo "cppcms $(cat $SCRIPT_DIR/../VERSION)"
#  exit 0
#else
#  echo "no"
#  exit 1
#fi
